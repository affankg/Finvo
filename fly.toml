# fly.toml app configuration file generated for finvo-1vyg1q on 2025-08-24T10:55:44Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'finvo-1vyg1q'
primary_region = 'sin'

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[deploy]
  strategy = "immediate"

[checks]
  [checks.health]
    grace_period = "90s"
    interval = "60s"
    method = "GET"
    path = "/health/"
    port = 8080
    timeout = "10s"
    type = "http"

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
  swap_size_mb = 512
